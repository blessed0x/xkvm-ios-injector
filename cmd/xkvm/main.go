// Command xkvm is the entry point for xKVM, the iOS app modifier & tweak
// injector (cyan + Azule heritage, rewritten in Go).
package main

import "github.com/blessed0x/xkvm-ios-injector/internal/cli"

func main() {
	cli.Main()
}
